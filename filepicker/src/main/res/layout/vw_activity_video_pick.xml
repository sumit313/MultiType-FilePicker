<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <include android:id="@+id/tb_pick"
        layout="@layout/vw_layout_top_bar" />

    <View
        android:id="@+id/rv_top_divider"
        android:layout_width="match_parent"
        android:layout_height="2dp"
        android:layout_below="@+id/tb_pick"
        android:background="@color/vw_BgRv" />
    <androidx.constraintlayout.widget.ConstraintLayout
        android:id="@+id/cl_manage"
        android:layout_below="@id/rv_top_divider"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/vw_BgRv">
        <TextView
            android:id="@+id/txt_manage_info"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            android:layout_marginStart="5dp"
            android:layout_marginEnd="15dp"
            android:maxLines="2"
            android:ellipsize="end"
            android:layout_marginVertical="5dp"
            android:textColor="@android:color/darker_gray"
            android:text="You've given access to a select number of photos and videos."
            app:layout_constraintEnd_toStartOf="@id/txt_manage"
            />
        <TextView
            android:id="@+id/txt_manage"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:layout_constraintEnd_toEndOf="parent"
            android:text="Manage"
            android:textColor="@android:color/white"
            android:textSize="16sp"
            android:layout_marginEnd="10dp"
            app:layout_constraintTop_toTopOf="@id/txt_manage_info"
            app:layout_constraintBottom_toBottomOf="@id/txt_manage_info"/>
    </androidx.constraintlayout.widget.ConstraintLayout>

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rv_video_pick"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@id/cl_manage"
        android:background="@color/vw_BgRv" />

    <ProgressBar android:id="@+id/pb_video_pick"
        android:layout_height="wrap_content"
        android:layout_width="wrap_content"
        android:layout_centerInParent="true" />

</RelativeLayout>
